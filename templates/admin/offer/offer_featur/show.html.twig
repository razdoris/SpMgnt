{% extends 'base.html.twig' %}

{% block title %}PresentationOfferFeature{% endblock %}

{% block body %}
    <h1>PresentationOfferFeature</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presentation_offer_feature.id }}</td>
            </tr>
            <tr>
                <th>FeatureName</th>
                <td>{{ presentation_offer_feature.featureName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_offer_offer_featur_index') }}">back to list</a>

    <a href="{{ path('admin_offer_offer_featur_edit', {'id': presentation_offer_feature.id}) }}">edit</a>

    {{ include('admin/offer/offer_featur/_delete_form.html.twig') }}
{% endblock %}
