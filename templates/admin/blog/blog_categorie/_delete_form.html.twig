<form method="post" action="{{ path('admin_blog_blog_categorie_delete', {'id': presentation_blog_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presentation_blog_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
