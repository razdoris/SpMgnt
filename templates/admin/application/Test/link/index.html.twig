<!DOCTYPE html>

<title>ApplicationTestTestValue index</title>

{% block body %}
    <h1>ApplicationTestTestValue index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for application_test_test_value in application_test_test_values %}
            <tr>
                <td>{{ application_test_test_value.id }}</td>
                <td>{{ application_test_test_value.name }}</td>
                <td>
                    <a href="{{ path('admin_application_test_link_show', {'id': application_test_test_value.id}) }}">show</a>
                    <a href="{{ path('admin_application_test_link_edit', {'id': application_test_test_value.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


    {{ include('admin/_inc/_footerIndex.html.twig',{'pathUrl':'admin_application_test_link'}) }}

{% endblock %}
