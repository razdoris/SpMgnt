<form method="post" action="{{ path(pathUrl, {'id': id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ id) }}">
    <button class="button delete">Delete</button>
</form>
