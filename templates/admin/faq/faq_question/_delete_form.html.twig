<form method="post" action="{{ path('admin_faq_faq_question_delete', {'id': presentation_faq_question.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presentation_faq_question.id) }}">
    <button class="btn">Delete</button>
</form>
